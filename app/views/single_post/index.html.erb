<%= render 'shared/topnav' %>

<div class="container">	
	<div class="row">

		<!-- sidebar -->
		<%= render 'shared/sidebar' %>

		<div class="col-md-10">
			<% @postsL.each do |postL| %>
			<h1><%= postL.name %></h1>
			<p><%= raw postL.content %></p>
			<br>
		</div>
	<% end %>
	</div>	
</div>